词汇表：

时间相关：
1、时间刻度
    ruler,agent都会记录自己的时间刻度
    时间刻度是进行时间同步的依据
    当agent的时间刻度小于ruler的刻度时，ruler会同意其获取时间片
    agent获取到时间片后，应该更新自身的状态，并在最后*确保*更新自身的时间刻度(否则该agent相当于作弊，下一次还可以从ruler获取
    到时间片)(当然这可以是一些特殊技能的实现)
2、时间片